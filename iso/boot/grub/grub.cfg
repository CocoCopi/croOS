set timeout=3
set default=0

insmod all_video

menuentry "croOS 4.0 HyperCorros" {
    insmod multiboot
    multiboot /boot/croOS.elf
    boot
}

menuentry "croOS 4.0 - Serial Debug" {
    insmod multiboot
    multiboot /boot/croOS.elf
    set serial_console=true
    boot
}
